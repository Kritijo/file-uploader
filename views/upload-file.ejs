<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/root.css" />
    <link rel="stylesheet" href="/form.css" />
    <title>Upload File</title>
  </head>
  <body>
    <form <% if(folderId){ %> action="/folder/<%= folderId %>/upload-file" <% } else{action="/upload-file"} %> method="POST" enctype="multipart/form-data">
      <h1>Upload Your File</h1>
      <label for="file">Choose a file:</label>
      <input
        type="file"
        id="file"
        name="file"
        accept=".pdf,.doc,.docx,.xls,.xlsx,.ppt,.pptx,.txt,.jpg,.jpeg,.png,.gif,.svg,.mp4,.webm,.mp3,.wav,.zip,.rar"
        required
      />
      <button type="submit">Upload</button>
    </form>
  </body>
</html>
